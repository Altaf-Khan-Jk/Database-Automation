# Copy this file to .env and fill in your local MySQL credentials.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=yourpassword
DB_NAME=nyc_taxi
# Optional: path to mysqldump if it's not in PATH
MYSQLDUMP_PATH=mysqldump
